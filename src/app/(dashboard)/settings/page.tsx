import { SettingsPanel } from "@/app/components/layout/SettingsPanel";

export default function SettingsPage() {
  return (
    <div className="flex items-center justify-center py-8">
      <SettingsPanel />
    </div>
  );
}
