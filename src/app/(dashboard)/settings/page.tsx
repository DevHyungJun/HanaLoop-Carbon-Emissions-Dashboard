import { SettingsPanel } from "@/app/components/layout/SettingsPanel";

export default function SettingsPage() {
  return (
    <div className="flex min-h-[calc(100dvh-3.5rem)] items-center justify-center py-8">
      <SettingsPanel />
    </div>
  );
}
